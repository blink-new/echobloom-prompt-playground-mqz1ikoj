import { createClient } from '@blinkdotnew/sdk'

export const blink = createClient({
  projectId: 'echobloom-prompt-playground-mqz1ikoj',
  authRequired: false // No authentication needed for this app
})